<?php
// Auto-generated by folder-server. Front-controller router for `php -S`.
// Serves an existing static file (or real .php) as-is; routes every other
// request to the front controller — the equivalent of the classic .htaccess
// "send unknown URLs to index.php" rewrite used by WordPress, Laravel, etc.
$root = rtrim($_SERVER['DOCUMENT_ROOT'], '/');
$path = urldecode(parse_url($_SERVER['REQUEST_URI'], PHP_URL_PATH));
if ($path !== '/' && strpos($path, '..') === false && is_file($root . $path)) {
    return false;
}
require $root . '/{{FRONT_CONTROLLER}}';
